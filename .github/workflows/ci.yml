name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  # ── Format check ────────────────────────────────────────
  fmt:
    name: fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  # ── Clippy ──────────────────────────────────────────────
  clippy:
    name: clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --workspace --exclude agentguard-ebpf --all-targets -- -D warnings

  # ── No unwrap/panic guard ───────────────────────────────
  no-panic:
    name: panic-free
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/check-no-panic.sh

  # ── Test matrix (per OS) ────────────────────────────────
  test:
    name: test (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Build (Linux)
        if: runner.os == 'Linux'
        run: cargo build --workspace --exclude agentguard-ebpf
      - name: Build (Windows)
        if: runner.os == 'Windows'
        run: cargo build --workspace --exclude agentguard-ebpf --exclude agentguard-linux
      - name: Test (Linux)
        if: runner.os == 'Linux'
        run: cargo test --workspace --exclude agentguard-ebpf
      - name: Test (Windows)
        if: runner.os == 'Windows'
        run: cargo test --workspace --exclude agentguard-ebpf --exclude agentguard-linux

  # ── Linux daemon build ──────────────────────────────────
  linux-daemon:
    name: linux-daemon
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo build -p agentguard-linux

  # ── eBPF bytecode (nightly) ─────────────────────────────
  ebpf-build:
    name: ebpf (nightly)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@nightly
        with:
          components: rust-src
      - name: Add bpf target
        run: rustup +nightly target add bpfel-unknown-none || true
      - uses: Swatinem/rust-cache@v2
      - name: Build eBPF bytecode
        working-directory: crates/agentguard-ebpf
        run: |
          if rustup +nightly target list --installed | grep -q bpfel-unknown-none; then
            cargo +nightly build --release --target bpfel-unknown-none -Z build-std=core
          else
            echo "SKIP: bpfel-unknown-none target not available for this nightly"
          fi

  # ── CLI integration test (build + init) ─────────────────
  cli-test:
    name: cli
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo build -p agentguard-cli
      - name: Test init generates valid config
        run: |
          cargo run -q -p agentguard-cli -- init --output /tmp/ag-test.toml
          grep -q "protected_dirs" /tmp/ag-test.toml || exit 1
          grep -q "AgentGuard" /tmp/ag-test.toml || exit 1
          rm /tmp/ag-test.toml
      - name: Test init rejects existing file
        run: |
          echo "x" > /tmp/existing.toml
          cargo run -q -p agentguard-cli -- init --output /tmp/existing.toml && exit 1 || true

  # ── Installer build ─────────────────────────────────────
  installer:
    name: installer
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo build -p agentguard-installer
